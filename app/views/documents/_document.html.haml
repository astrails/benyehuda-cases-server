%p
  != link_to document.file_file_name, document.file.url
  = "(#{sprintf("%.2f", document.file_file_size.to_f/1024)} KB)"
  Uploaded by
  != link_to h(document.user.name), profiles_path(document.user)
  at
  = document.created_at.to_s(:db)
  - if document_can_be_deleted_by?(document, current_user)
    != link_to "Remove", task_document_path(document.task_id, document), :method => :delete, :confirm => "Are you sure?"