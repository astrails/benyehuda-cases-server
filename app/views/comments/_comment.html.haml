%div[comment]{:class => class_for_comment(comment)}
  != "By %{name} %{time} ago" % {:name => link_to(h(comment.user.name), profiles_path(comment.user)), :time => time_ago_in_words(comment.created_at)}
  %p
    = comment.message
  