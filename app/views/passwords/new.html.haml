- set_tab :profile, _("Change Password")

- semantic_form_for(@user ||= User.new, :url => passwords_path) do |f|
  != f.input :email, :label => _("Email")
  != f.submit _("Send Password Reset Instructions")

!= link_to _("login"), login_path, :class => "login"