#simplemodal-container.simplemodal-container
  %ul
  - collection.each do |k|
    %li
      = link_to "Delete", admin_task_kind_path(k), :remote => true, :method => :delete
      = k.name
  %p
    = link_to "New", new_admin_task_kind_path, :remote => true, :method => :post
