- form_for(@global_preference) do |f|

  - if @global_preference.errors.any?
    #error_explanation
      %h2
        = pluralize(@global_preference.errors.count, "error")
        prohibited this post from being saved:

      %ul
        - @post.errors.full_messages.each do |msg|
          %li= msg

  != f.text_field :name
  != f.text_field :value
  != f.text_field :ttl

  != f.submit
